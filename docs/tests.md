# Tool tests

* compare different version

# File system tests

* get volume identifier, if available
* get volume label
* list directory
  * directories with different number of entries
* get file system entry metadata
  * using file system identifier
  * using path
* read contents of data stream
  * file contents (default data stream)
  * alternative data streams
* list extended attributes
* read contents of extended attribute
